# ======================================
# FutureIntern Backend - Environment Variables
# ======================================
# SECURITY NOTICE: Never commit actual secrets to version control!
# This is a template - copy and customize for your environment

# ======================================
# JWT Configuration (REQUIRED FOR SECURITY)
# ======================================
# CRITICAL: Generate a secure random key using:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
# DO NOT use the default value in production!
JWT_SECRET_KEY=CHANGE_ME_TO_SECURE_RANDOM_KEY_IN_PRODUCTION

# ======================================
# Flask Environment
# ======================================
FLASK_ENV=production
FLASK_DEBUG=0

# Port (Railway/Heroku will set this automatically)
PORT=5000

# ======================================
# Database Configuration
# ======================================
# Development (SQLite - local only)
# DATABASE_URL=sqlite:///futureintern.db

# Production (PostgreSQL - recommended)
# Railway auto-provides this if you add PostgreSQL plugin
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@host:5432/futureintern

# ======================================
# Frontend URL for CORS
# ======================================
# Update with your deployed frontend URL (Vercel, Netlify, etc.)
# Multiple origins: FRONTEND_URL=https://app.com,https://www.app.com
FRONTEND_URL=https://your-vercel-app.vercel.app

# ======================================
# Email Configuration (Password Reset)
# ======================================
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
# For Gmail, create App Password: https://support.google.com/accounts/answer/185833
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-gmail-app-password
MAIL_DEFAULT_SENDER=noreply@futureintern.com

# ======================================
# Security Settings
# ======================================
# Password policy
MIN_PASSWORD_LENGTH=8
REQUIRE_PASSWORD_COMPLEXITY=true

# File upload limits (in bytes)
MAX_CONTENT_LENGTH=5242880

# ======================================
# DEPLOYMENT CHECKLIST
# ======================================
# [ ] Generate and set strong JWT_SECRET_KEY
# [ ] Configure PostgreSQL database (not SQLite in production)
# [ ] Set actual FRONTEND_URL to your deployed app
# [ ] Configure email credentials for password reset
# [ ] Enable HTTPS/TLS on your hosting platform
# [ ] Set FLASK_ENV=production and FLASK_DEBUG=0
# [ ] Test all environment variables are loaded correctly
# [ ] Never commit .env file with real secrets to git!

