# Example environment variables for Railway deployment
# Copy these to Railway's environment variables section

# JWT Secret Key (IMPORTANT: Use a strong random key in production)
JWT_SECRET_KEY=your-secret-key-change-in-production-flask-jwt-2024

# Flask Environment
FLASK_ENV=production

# Port (Railway will automatically set this)
PORT=5000

# Frontend URL (Update with your Vercel URL after deployment)
FRONTEND_URL=https://your-vercel-app.vercel.app

# Database URL (if using PostgreSQL on Railway, uncomment and configure)
# DATABASE_URL=postgresql://user:password@host:port/database
